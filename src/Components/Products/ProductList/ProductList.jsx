import React from "react";
import ProductCard from "../ProductCard/ProductCard";

const ProductList = () => {
  return (
    <div>
      <ProductCard />
    </div>
  );
};

export default ProductList;
